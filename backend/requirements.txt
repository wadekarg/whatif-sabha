fastapi==0.115.0
uvicorn[standard]==0.30.6
pdfplumber==0.11.4

# LangChain ecosystem — keep these in sync
langchain==1.2.15
langchain-core==1.2.26
langchain-google-genai==4.2.1
langchain-groq==1.1.2
langchain-cerebras==0.6.0
langchain-anthropic>=0.3.0
langgraph==1.1.6
langchain-openai>=0.3.0
edge-tts>=7.0.0

# Vector DB
chromadb==0.5.23
# Override system sqlite3 only on Linux. Wheels aren't published for macOS / Windows
# (their system sqlite3 is already new enough for ChromaDB). Skip on Python 3.13+
# until pysqlite3-binary publishes wheels for it.
pysqlite3-binary==0.5.4.post2; sys_platform == "linux" and python_version < "3.13"
sentence-transformers==3.3.1
# Pin numpy<2 — PyTorch 2.x and the sentence-transformers wheels are built
# against NumPy 1.x ABI. NumPy 2.x triggers
# "A module compiled using NumPy 1.x cannot be run in NumPy 2.x" + a
# RuntimeError("Numpy is not available") at embedding time.
numpy<2

# Database
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0

# Web research
beautifulsoup4==4.12.3
lxml==5.3.0

# Utils
python-multipart==0.0.12
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
aiofiles==24.1.0
redis==5.2.1
httpx==0.28.1

# --- Character Soul Memory ---
# Graphiti: temporal knowledge graph for persistent character memory across debates
# Neo4j driver required (Neo4j server via docker-compose or standalone)
graphiti-core>=0.3.0
neo4j>=5.0.0

# --- Narrative Causal Graph ---
# LightRAG: extracts causal knowledge graphs from story text
# Proven on literary texts (Dickens, etc.) — outperforms Microsoft GraphRAG on fiction
# Install separately if you want LightRAG (large dependency):
# pip install lightrag-hku
# lightrag-hku  # optional — uncomment to enable
